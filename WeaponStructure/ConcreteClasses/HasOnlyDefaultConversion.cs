namespace PFDB.WeaponStructure
{/*
	public class HasOnlyDefaultConversion : Weapon
	{
		private protected IDefaultConversion _defaultConversion;

		/*
		public HasOnlyDefaultConversion(string name, IDefaultConversion defaultConversion, Categories category) : base(name,defaultConversion.StatisticCollection, category)
		{
			_defaultConversion = defaultConversion;
		}

		public HasOnlyDefaultConversion(string name, string? description, IDefaultConversion defaultConversion, Categories category) : base(name, description, defaultConversion.StatisticCollection, category)
		{
			_defaultConversion = defaultConversion;
		}*
	}
*/



}
